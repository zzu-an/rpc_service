# v0.5 RPC 微服务 Spec

## 目标
将单体逐步拆成边界明确的服务，并处理远程调用失败。

## 允许
- gRPC
- Protobuf
- etcd
- 超时
- 重试（仅幂等操作）
- 熔断

## 推荐拆分
- user
- product
- order
- inventory

## 禁止
- 服务之间直接访问对方数据库

## 必须验证
- RPC 超时生效
- 依赖服务不可用时不会无限阻塞
- 重试不会破坏幂等
- 服务发现异常可观测
